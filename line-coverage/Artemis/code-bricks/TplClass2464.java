public class TplClass2464 {

    private static final void method(boolean x, boolean[] a) throws Throwable {
        for (// NOTE: bitwise and, not the common &&
        // NOTE: bitwise and, not the common &&
        int i = 0; // NOTE: bitwise and, not the common &&
        i < 128; i++) a[i] &= x;
    }
}

