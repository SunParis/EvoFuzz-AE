import java.net.Socket;

public class TplClass6745 {

    private static final void method(java.net.Socket soc) throws Throwable {
        soc.bind(null);
        if (!soc.isBound())
            ;
        if (soc.getLocalPort() <= 0)
            ;
        if (soc.getLocalAddress() == null)
            ;
    }
}

