import java.net.DatagramPacket;

public class TplClass5282 {

    private static final void method(int length, byte[] buf, int offset, java.net.DatagramPacket packet) throws Throwable {
        if (packet.getData() != buf || packet.getOffset() != offset || packet.getLength() != length) {
        }
    }
}

