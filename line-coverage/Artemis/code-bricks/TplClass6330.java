import java.security.cert.Certificate;

public class TplClass6330 {

    private static final void method(java.security.cert.Certificate[] chain, java.security.cert.Certificate[] gotChain) throws Throwable {
        if (gotChain.length == 2 && gotChain[0] == chain[0] && gotChain[1] == chain[1]) {
        } else {
        }
    }
}

