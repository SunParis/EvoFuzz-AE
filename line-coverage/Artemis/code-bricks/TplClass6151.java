import java.math.RoundingMode;
import java.math.BigDecimal;

public class TplClass6151 {

    private static final void method() throws Throwable {
        if (RoundingMode.valueOf(BigDecimal.ROUND_DOWN) != RoundingMode.DOWN) {
        }
    }
}

