public class TplClass1000 {

    private static final void method(java.lang.Thread myInitialThread) throws Throwable {
        if (myInitialThread == null) {
            myInitialThread = Thread.currentThread();
        } else if (myInitialThread != Thread.currentThread()) {
        }
    }
}

