import java.nio.CharBuffer;

public class TplClass7650 {

    private static final void method(java.nio.CharBuffer dupe) throws Throwable {
        if (dupe.charAt(0) != 't' || dupe.charAt(1) != 'e' || dupe.charAt(2) != 's' || dupe.charAt(3) != 't') {
        }
    }
}

