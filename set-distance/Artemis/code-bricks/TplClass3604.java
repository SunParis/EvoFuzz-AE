import java.util.concurrent.atomic.AtomicInteger;

public class TplClass3604 {

    private static final void method(int winners, int NUMBER_OF_THREADS, java.util.concurrent.atomic.AtomicInteger[] targetted) throws Throwable {
        // it's non-deterministic.
        if (winners != 1) {
            for (int i = 0; i < NUMBER_OF_THREADS; ++i) {
            }
        }
    }
}

