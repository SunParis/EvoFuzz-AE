import java.nio.charset.CharsetEncoder;

public class TplClass63 {

    private static final void method(char c, java.nio.charset.CharsetEncoder enc) throws Throwable {
        if (!enc.canEncode(c)) {
        }
    }
}

