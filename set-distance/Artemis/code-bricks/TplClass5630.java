import java.nio.channels.AsynchronousServerSocketChannel;

public class TplClass5630 {

    private static final void method(java.nio.channels.AsynchronousServerSocketChannel ch) throws Throwable {
        // shutdownNow is required to close all channels
        if (ch.isOpen())
            ;
    }
}

