import java.nio.charset.Charset;

public class TplClass5106 {

    private static final void method(java.lang.String str, java.nio.charset.Charset charset, byte[] bytes) throws Throwable {
        /* String(byte[] bs, Charset charset) */
        if (!(new String(bytes, charset).equals(str)))
            ;
    }
}

