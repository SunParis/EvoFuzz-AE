import java.text.NumberFormat;

public class TplClass4922 {

    private static final void method(java.lang.String str, java.text.NumberFormat nf, double d, double seed) throws Throwable {
        d = d + 1.0d + seed;
        str = nf.format(d);
    }
}

