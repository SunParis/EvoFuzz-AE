import java.nio.channels.DatagramChannel;
import java.nio.channels.ServerSocketChannel;

public class TplClass5027 {

    private static final void method(java.nio.channels.ServerSocketChannel listener, int port, java.nio.channels.DatagramChannel peer) throws Throwable {
        listener = null;
        peer = null;
        port = -1;
    }
}

