public class TplClass5078 {

    private static final void method(int i, java.lang.String[] tests) throws Throwable {
        String s = tests[i];
        int len = s.length();
        boolean empty = s.isEmpty();
        if ((len != 0 && empty) || (len == 0 && !empty))
            ;
    }
}

