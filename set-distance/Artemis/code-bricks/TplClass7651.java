import java.nio.CharBuffer;

public class TplClass7651 {

    private static final void method(boolean marked, java.nio.CharBuffer slice, java.nio.CharBuffer buff) throws Throwable {
        if (marked || slice.position() != 0 || buff.remaining() != slice.limit() || buff.remaining() != slice.capacity()) {
        }
    }
}

