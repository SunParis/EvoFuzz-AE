import java.nio.charset.Charset;

public class TplClass5113 {

    private static final void method(java.nio.charset.Charset charset, java.lang.String end, byte[] bytes) throws Throwable {
        if (!(new String(bytes, 2, bytes.length - 2, charset).equals(end)))
            ;
    }
}

