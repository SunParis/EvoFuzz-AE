public class TplClass4263 {

    private static final void method(int[] a) throws Throwable {
        // As above, but now the loop control also wraps around.
        for (byte i = 0; i < a.length; i++) {
            a[i] = -i;
        }
    }
}

