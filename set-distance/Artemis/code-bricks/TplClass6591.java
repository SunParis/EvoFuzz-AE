import java.util.Map;

public class TplClass6591 {

    private static final void method(int i, java.util.Map m) throws Throwable {
        m.put(new Integer(i), "");
        int eldest = ((Integer) m.keySet().iterator().next()).intValue();
        if (eldest != Math.max(i - 9, 0))
            ;
    }
}

