public class LongExprs {

    // $mut11: Math.addExact(a, b)
    public static long $mut11(long $lval1, long $lval2) {
        return (Math.addExact($lval1, $lval2));
    }
    
    // $mut12: Math.subtractExact(a, b)
    public static long $mut12(long $lval1, long $lval2) {
        return (Math.subtractExact($lval1, $lval2));
    }

    // $mut13: Math.multiplyExact(a, b)
    public static long $mut13(long $lval1, long $lval2) {
        return (Math.multiplyExact($lval1, $lval2));
    }

}
