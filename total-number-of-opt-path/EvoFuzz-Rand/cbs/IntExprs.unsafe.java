public class IntExprs {

    // $mut17: Math.addExact(a, b)
	public static int $mut17(int $lval1, int $lval2) {
		return Math.addExact($lval1, $lval2);
	}

	// $mut18: Math.subtractExact(a, b)
	public static int $mut18(int $lval1, int $lval2) {
		return Math.subtractExact($lval1, $lval2);
	}

    // $mut19: Math.multiplyExact(a, b)
	public static int $mut19(int $lval1, int $lval2) {
		return Math.multiplyExact($lval1, $lval2);
	}

}
