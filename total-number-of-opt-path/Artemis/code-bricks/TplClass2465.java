public class TplClass2465 {

    private static final void method(boolean x, boolean[] a) throws Throwable {
        for (// NOTE: bitwise or, not the common ||
        // NOTE: bitwise or, not the common ||
        int i = 0; // NOTE: bitwise or, not the common ||
        i < 128; i++) a[i] |= x;
    }
}

