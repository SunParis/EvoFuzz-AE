public class TplClass4180 {

    private static final void method(int result, int x, int y) throws Throwable {
        // As a result, the call itself can be moved out of the loop body.
        result += Math.max(x, y);
    }
}

