public class TplClass2627 {

    private static final void method(boolean field) throws Throwable {
        // 'incoming' must only have a use in the inner loop.
        boolean incoming = field;
        while (incoming) {
        }
    }
}

