import java.util.Locale;

public class TplClass7458 {

    private static final void method(int i, java.util.Locale[] locales) throws Throwable {
        if (locales[i].getCountry().length() == 0)
            ;
        else {
            if (locales[i].getVariant().length() == 0)
                ;
        }
    }
}

