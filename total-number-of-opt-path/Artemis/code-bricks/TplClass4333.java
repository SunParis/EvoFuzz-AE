public class TplClass4333 {

    private static final void method(int result, int[] q, int i, java.lang.Integer[] z) throws Throwable {
        // z[0] itself during BCE on q[i]. Therefore, we just check BCE on q[i].
        result += q[i] + z[0];
    }
}

