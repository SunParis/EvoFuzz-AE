import java.math.RoundingMode;
import java.math.BigDecimal;

public class TplClass6155 {

    private static final void method() throws Throwable {
        if (RoundingMode.valueOf(BigDecimal.ROUND_HALF_UP) != RoundingMode.HALF_UP) {
        }
    }
}

