import java.util.Arrays;

public class TplClass75 {

    private static final void method(boolean enc_res, byte[] b, byte[] bt, int size, long start, long end, boolean failed) throws Throwable {
        if (!enc_res || !Arrays.equals(b, bt)) {
            failed = true;
        } else {
        }
    }
}

