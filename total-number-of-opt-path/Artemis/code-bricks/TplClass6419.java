import java.nio.channels.SelectionKey;

public class TplClass6419 {

    private static final void method(java.nio.channels.SelectionKey sk) throws Throwable {
        if (sk.isValid())
            ;
        if (sk.channel().isOpen())
            ;
    }
}

