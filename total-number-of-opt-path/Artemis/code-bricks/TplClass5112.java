import java.nio.charset.Charset;

public class TplClass5112 {

    private static final void method(java.lang.String start, java.nio.charset.Charset charset, byte[] bytes) throws Throwable {
        if (!(new String(bytes, 0, 2, charset).equals(start)))
            ;
    }
}

