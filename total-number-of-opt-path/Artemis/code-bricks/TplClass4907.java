public class TplClass4907 {

    private static final void method(double fractional, int j, double d) throws Throwable {
        if (j >= 0)
            d = (double) j + fractional;
        else
            d = (double) j - fractional;
    }
}

