public class TplClass4150 {

    private static final void method(boolean doLog) throws Throwable {
        // diff in address so lets skip every 2:nd address so it is null
        if (doLog) {
        }
    }
}

